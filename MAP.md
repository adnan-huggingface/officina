# Map

Generated by `cargo xtask map`, and again by every `cargo xtask check` — do not edit by hand. Where each thing is, how to run its tests, and every comment that states what an application was measured to do. The *why* of each is at the place it points to.

## Crates

| crate | directory | tests | what |
|---|---|---|---|
| `calx` | `crates/app-calx` | `-p calx --lib` `-p calx --bin calx` | Calx — the spreadsheet application. |
| `scriva` | `crates/app-scriva` | `-p scriva --lib` `-p scriva --bin scriva` `--test anchors` `--test charts_on_paper` `--test print_smoke` `--test zz_probe` | Scriva — the word processor. |
| `brand` | `crates/brand` | `-p brand --lib` | The applications' icons, drawn in code. |
| `cfb-reader` | `crates/cfb` | `-p cfb-reader --lib` | Compound File Binary (MS-CFB) reader. |
| `chart` | `crates/chart` | `-p chart --lib` | Office charts: the model both applications share, and the reader for the part they both carry. |
| `dpi-guard` | `crates/dpi-guard` | `-p dpi-guard --lib` | Keeping Windows from fighting the user over a dragged window. |
| `metafile` | `crates/metafile` | `-p metafile --lib` | Playing back a Windows metafile, so that a drawing made of records becomes a drawing made of ink. |
| `ooxml` | `crates/ooxml` | `-p ooxml --lib` | Open Packaging Conventions container handling, and the Preservation Vault. |
| `ss-csv` | `crates/ss-csv` | `-p ss-csv --lib` `--test typed_import` | CSV and TSV: sniffing what a file actually is, and reading it without holding it all in memory. |
| `ss-formula` | `crates/ss-formula` | `-p ss-formula --lib` `--test conformance` `--test editing_session` `--test scale` `--test spilling` | Formula lexer, parser, dependency graph, evaluation, and the function library. |
| `ss-model` | `crates/ss-model` | `-p ss-model --lib` | Spreadsheet document model: workbook, sheets, sparse cell store, styles. |
| `ss-xls` | `crates/ss-xls` | `-p ss-xls --lib` | Reading legacy `.xls` workbooks — Excel 97 through 2003 — into the [`ss_model`] document model. |
| `ss-xlsx` | `crates/ss-xlsx` | `-p ss-xlsx --lib` `--test charts` `--test comments` `--test large_workbook` `--test new_workbook` `--test outline` `--test pictures` `--test recalc_against_excel` `--test rules` `--test sheet_lifecycle` | Reading xlsx workbooks into the [`ss_model`] document model. |
| `ui-kit` | `crates/ui-kit` | `-p ui-kit --lib` | Shared application shell for Calx and Scriva. |
| `wp-compare` | `crates/wp-compare` | `-p wp-compare --bin wp-compare` `--test without_office` | Where Scriva's page differs from Word's, ranked. |
| `wp-doc` | `crates/wp-doc` | `-p wp-doc --lib` `--test corpus` | Reading Word 97–2003 `.doc` files. |
| `wp-docx` | `crates/wp-docx` | `-p wp-docx --lib` `--test corpus` `--test office_templates` `--test writer` | `.docx` reader and writer, over `ooxml` and `wp-model`. |
| `wp-layout` | `crates/wp-layout` | `-p wp-layout --lib` `--test documents` | Text layout: shaping, line breaking, and pagination. |
| `wp-model` | `crates/wp-model` | `-p wp-model --lib` | Word document model: paragraph/run tree, styles, sections, numbering, tables. |
| `wp-odf` | `crates/wp-odf` | `-p wp-odf --lib` `--test watermark` | Reading OpenDocument text documents — `.odt`. |
| `wp-print` | `crates/wp-print` | `-p wp-print --lib` | Putting laid-out pages on paper: a PDF for a file, GDI for a printer. |
| `wp-text` | `crates/wp-text` | `-p wp-text --lib` | Plain text and Markdown, read and written. |

## Files

Every source file, its size, and the first sentence of its module doc.

### `calx`

- `crates/app-calx/src/clock.rs` (31) The wall clock, read in the user's own time zone.
- `crates/app-calx/src/dialogs.rs` (1921) Calx's boxes: every dialog the grid can put up, drawn from the draft in `Calx::dialog`, and the tabs of Format Cells.
- `crates/app-calx/src/grid/axis.rs` (533) Where rows and columns sit, and which of them a viewport can see.
- `crates/app-calx/src/grid/chart.rs` (248) Where a chart sits on the grid, and what numbers it plots.
- `crates/app-calx/src/grid/editor.rs` (468) The cell editor: what is being typed, and how it is coloured.
- `crates/app-calx/src/grid/mod.rs` (1771) The spreadsheet grid.
- `crates/app-calx/src/grid/paint.rs` (5666) Drawing the grid, and the mouse and keyboard that drive it.
- `crates/app-calx/src/grid/picture.rs` (431) Anchored pictures: decoding them, drawing them, and handling them.
- `crates/app-calx/src/grid/selection.rs` (675) What is selected, and how the keyboard and mouse change it.
- `crates/app-calx/src/icons.rs` (590) Toolbar icons, drawn rather than typed.
- `crates/app-calx/src/inspector.rs` (753) The chart inspector: a panel beside the grid for as long as a chart is selected.
- `crates/app-calx/src/lib.rs` (11) Calx — the spreadsheet application.
- `crates/app-calx/src/main.rs` (5714) Calx — spreadsheet.
- `crates/app-calx/src/tests.rs` (1026) 

### `scriva`

- `crates/app-scriva/src/app/bands.rs` (590) Headers and footers — the bands above and below the text of a page: which one the caret is in, making one a section does not have yet, first-page and odd/even kinds, linking to the previous section, the band bar, and …
- `crates/app-scriva/src/app/context.rs` (208) The page's right-click menu: what a click or Shift+F10 on the document offers, drawn on the same card as every other menu.
- `crates/app-scriva/src/app/dialogs.rs` (919) Scriva's boxes: Insert Table, colour, paragraph, column width, picture size, fonts and zoom.
- `crates/app-scriva/src/app/find_bar.rs` (278) The find bar: one bar for Find and Replace, on the row under the toolbar, which holds the keyboard while it is open and hands it back to the document when it closes.
- `crates/app-scriva/src/app/font_dialog.rs` (399) Format ▸ Font… (Ctrl+D): family, style, size, colour, highlight and the effects the model writes, with a line of preview, applied to the selection as one undo step.
- `crates/app-scriva/src/app/goto.rs` (170) Edit ▸ Go To… (Ctrl+G): a small popover on the status bar's page count.
- `crates/app-scriva/src/app/help.rs` (147) The Help menu's boxes: every key the application answers to, generated from the command table so that it cannot drift; the user guide, opened beside the executable; and About.
- `crates/app-scriva/src/app/notices.rs` (151) The three tiers of telling the user something, and the two lighter ones drawn here: a status notice, which is a sentence at the left of the status bar for four seconds, and a notice, which takes the row under the tool…
- `crates/app-scriva/src/app/page_setup.rs` (319) Layout ▸ Page Setup…: paper, orientation, margins and the bands' distance from the edge in one box, with the page drawn from the numbers as they are typed.
- `crates/app-scriva/src/app/strips.rs` (339) The row under the toolbar, and the mode strips drawn on it.
- `crates/app-scriva/src/app/surface.rs` (968) The page surface: the scrolling desk the pages sit on, what the pointer does there — carets, selections, a picture picked, dragged and resized — and where on a page a point lands.
- `crates/app-scriva/src/app/tables.rs` (486) Editing a table: Tab from cell to cell, the edit every table command goes through, cell margins, column widths, and Insert Table itself.
- `crates/app-scriva/src/app/tests/bands.rs` (753) Headers, footers, watermarks and sections: the bands a page wears.
- `crates/app-scriva/src/app/tests/desk.rs` (1196) The desk as it is painted: pages, zoom, scrolling, the caret and the pointer.
- `crates/app-scriva/src/app/tests/editing.rs` (1127) Typing, keys in the text, lists, the clipboard, formatting and undo.
- `crates/app-scriva/src/app/tests/files.rs` (1366) Opening, saving, the formats between them, and what the window says about a file.
- `crates/app-scriva/src/app/tests/menus.rs` (743) Menus, shortcuts, boxes and the toolbar, reached the way a keyboard reaches them.
- `crates/app-scriva/src/app/tests/mod.rs` (189) Scriva's window, driven: one file per area, and here what more than one of them uses.
- `crates/app-scriva/src/app/tests/panes.rs` (461) The find bar and the panes beside the page: navigation, review and comments.
- `crates/app-scriva/src/app/tests/pictures.rs` (462) Pictures and charts: in by paste or menu, sized, moved and taken out.
- `crates/app-scriva/src/app/tests/tables.rs` (776) Tables: inserting, filling, merging, borders, rows and columns.
- `crates/app-scriva/src/app/watermark.rs` (424) The watermark: the box that asks for one, and the shape it becomes in a header, written the way Word writes its own so that Word finds it again.
- `crates/app-scriva/src/app/word_count.rs` (158) The Word Count box, from the status bar's count: pages, words, characters with and without spaces, paragraphs and lines — and, with a selection, the selection beside the document.
- `crates/app-scriva/src/app.rs` (5545) The application: the window, the commands, and the keys.
- `crates/app-scriva/src/author.rs` (536) A document written by the application itself, from a script of its own commands.
- `crates/app-scriva/src/clip.rs` (955) The formatted halves of a copy: CF_HTML and Rich Text Format.
- `crates/app-scriva/src/commands.rs` (407) Every command with a name and a key, in one table.
- `crates/app-scriva/src/drawings.rs` (353) A picture as an object: selected, moved, resized, deleted.
- `crates/app-scriva/src/edit.rs` (2164) Editing a document, and taking it back.
- `crates/app-scriva/src/find.rs` (319) Finding text in the document, and putting something else in its place.
- `crates/app-scriva/src/icons.rs` (439) Toolbar icons, drawn from lines rather than typed.
- `crates/app-scriva/src/lib.rs` (26) Scriva — the word processor.
- `crates/app-scriva/src/links.rs` (117) Following a hyperlink out of the document, or to somewhere inside it.
- `crates/app-scriva/src/main.rs` (31) Scriva — word processor.
- `crates/app-scriva/src/menus.rs` (745) The menu bar and the formatting row.
- `crates/app-scriva/src/panes/mod.rs` (12) The panes beside the page: Review on the right, and — from phase 5 of the redesign — Navigate on the left.
- `crates/app-scriva/src/panes/navigate.rs` (509) The Navigate pane: the document's headings as a tree, and its bookmarks.
- `crates/app-scriva/src/panes/review.rs` (781) The Review pane: every tracked change and comment as a card, in document order, with what can be done about each on the card itself.
- `crates/app-scriva/src/pictures.rs` (366) Decoding the drawings a document carries, and holding them for the painter.
- `crates/app-scriva/src/publish.rs` (196) What leaves the editor: the fonts and pictures a page renderer needs.
- `crates/app-scriva/src/revise.rs` (1517) Accepting and rejecting tracked changes, and recording new ones.
- `crates/app-scriva/src/shaper.rs` (869) Measuring text with the faces the application actually has.
- `crates/app-scriva/src/text.rs` (781) Editing the text of a paragraph.
- `crates/app-scriva/src/toolbar.rs` (1091) The toolbar: one row, on which the caret's state can be read at a glance.
- `crates/app-scriva/src/view.rs` (3145) The document surface: pages on a desk, a caret, and a selection.

### `brand`

- `crates/brand/src/lib.rs` (260) The applications' icons, drawn in code.
- `crates/brand/src/resource.rs` (86) The build-script half: writing the icon into a Windows executable.

### `cfb-reader`

- `crates/cfb/src/dir.rs` (161) The directory: 128-byte entries describing storages and streams.
- `crates/cfb/src/error.rs` (50) What can go wrong opening a compound file.
- `crates/cfb/src/fat.rs` (111) Sector allocation: the FAT, the DIFAT that indexes it, and chain walking.
- `crates/cfb/src/fixture.rs` (301) Builds compound files, for tests.
- `crates/cfb/src/header.rs` (115) The first 512 bytes, which say where everything else is.
- `crates/cfb/src/lib.rs` (192) Compound File Binary (MS-CFB) reader.
- `crates/cfb/src/tests.rs` (129) Read back what `fixture` lays out, and refuse what is not a compound file.

### `chart`

- `crates/chart/src/clipboard.rs` (78) The chart clipboard format the two applications exchange.
- `crates/chart/src/draw.rs` (2962) Where a chart's ink goes, decided once for every device that draws it.
- `crates/chart/src/lib.rs` (26) Office charts: the model both applications share, and the reader for the part they both carry.
- `crates/chart/src/model.rs` (520) Charts, and where they sit.
- `crates/chart/src/read.rs` (786) Chart parts: `xl/charts/chartN.xml`, `word/charts/chartN.xml`.

### `dpi-guard`

- `crates/dpi-guard/src/lib.rs` (312) Keeping Windows from fighting the user over a dragged window.

### `metafile`

- `crates/metafile/src/emf.rs` (1012) The Enhanced Metafile player.
- `crates/metafile/src/lib.rs` (93) Playing back a Windows metafile, so that a drawing made of records becomes a drawing made of ink.

### `ooxml`

- `crates/ooxml/src/compare.rs` (460) Semantic comparison of two packages — the engine behind the fidelity harness.
- `crates/ooxml/src/content_types.rs` (234) `[Content_Types].xml` — the map from part to MIME type.
- `crates/ooxml/src/error.rs` (72) Errors from package handling.
- `crates/ooxml/src/lib.rs` (65) Open Packaging Conventions container handling, and the Preservation Vault.
- `crates/ooxml/src/name.rs` (206) OPC part names.
- `crates/ooxml/src/package.rs` (576) The OPC package: a zip container of parts, plus the Preservation Vault.
- `crates/ooxml/src/rels.rs` (339) Relationship parts (`_rels/*.rels`) — the package's link graph.
- `crates/ooxml/src/xml.rs` (74) Small XML helpers shared by the package-level parsers.

### `ss-csv`

- `crates/ss-csv/src/encoding.rs` (245) Working out what bytes a text file is made of.
- `crates/ss-csv/src/lib.rs` (587) CSV and TSV: sniffing what a file actually is, and reading it without holding it all in memory.
- `crates/ss-csv/src/sheet.rs` (169) Between a delimited file and a [`Sheet`].

### `ss-formula`

- `crates/ss-formula/src/ast.rs` (137) The parsed shape of a formula.
- `crates/ss-formula/src/clip.rs` (1052) Copy, paste, and the fill handle.
- `crates/ss-formula/src/cond.rs` (1223) Deciding what a conditional format actually does to a cell.
- `crates/ss-formula/src/edit.rs` (1786) Editing a workbook, and being able to take it back.
- `crates/ss-formula/src/error.rs` (64) Parse errors.
- `crates/ss-formula/src/eval.rs` (599) Evaluation: turning a parsed expression into a value.
- `crates/ss-formula/src/filter.rs` (491) Applying an autofilter: which rows it leaves showing.
- `crates/ss-formula/src/find.rs` (448) Finding text in a workbook, and replacing it.
- `crates/ss-formula/src/functions/criteria.rs` (435) The criteria mini-language shared by `SUMIF`, `COUNTIF`, and their plural forms.
- `crates/ss-formula/src/functions/database.rs` (223) The database family: `DSUM`, `DGET`, and the rest.
- `crates/ss-formula/src/functions/date.rs` (950) Date and time functions.
- `crates/ss-formula/src/functions/dynamic.rs` (661) Dynamic arrays: functions whose result is a rectangle rather than a value.
- `crates/ss-formula/src/functions/engineering.rs` (401) Engineering functions: base conversion, bitwise operations, and the error function.
- `crates/ss-formula/src/functions/financial.rs` (681) Financial functions.
- `crates/ss-formula/src/functions/info.rs` (138) Information functions — the `IS*` family and friends.
- `crates/ss-formula/src/functions/logical.rs` (205) Logical functions.
- `crates/ss-formula/src/functions/lookup.rs` (799) Lookup and reference functions.
- `crates/ss-formula/src/functions/math.rs` (555) Math and trigonometry.
- `crates/ss-formula/src/functions/mod.rs` (199) The function library.
- `crates/ss-formula/src/functions/stats.rs` (627) Statistical functions.
- `crates/ss-formula/src/functions/text.rs` (655) Text functions.
- `crates/ss-formula/src/graph.rs` (797) The dependency graph and recalculation order.
- `crates/ss-formula/src/lexer.rs` (770) Tokenizer for Excel formula text.
- `crates/ss-formula/src/lib.rs` (47) Formula lexer, parser, dependency graph, evaluation, and the function library.
- `crates/ss-formula/src/parser.rs` (721) Pratt parser for Excel formulas.
- `crates/ss-formula/src/sheets.rs` (396) Adding, removing, renaming and reordering sheets.
- `crates/ss-formula/src/sort.rs` (863) Sorting a range, and finding the range a user meant.
- `crates/ss-formula/src/tools.rs` (480) Text to Columns and Remove Duplicates: two operations on a range that are neither formulas nor formatting.
- `crates/ss-formula/src/translate.rs` (810) Rewriting formula text when the grid moves under it.
- `crates/ss-formula/src/value.rs` (513) What a formula evaluates to, and Excel's rules for converting between types.
- `crates/ss-formula/src/workbook.rs` (704) Evaluating against a real [`Workbook`].

### `ss-model`

- `crates/ss-model/src/cell.rs` (386) Cell addresses and values.
- `crates/ss-model/src/color.rs` (325) Colours, and the four different ways a spreadsheet writes one down.
- `crates/ss-model/src/comment.rs` (67) Notes attached to cells.
- `crates/ss-model/src/cond.rs` (454) Conditional formatting and data validation.
- `crates/ss-model/src/datetime.rs` (272) Serial dates, and the leap year that never happened.
- `crates/ss-model/src/filter.rs` (230) Autofilters.
- `crates/ss-model/src/formula.rs` (101) Formulas as they are *stored*, not as they are evaluated.
- `crates/ss-model/src/lib.rs` (52) Spreadsheet document model: workbook, sheets, sparse cell store, styles.
- `crates/ss-model/src/numfmt.rs` (1453) Number formats: turning a stored value into the text a cell shows.
- `crates/ss-model/src/picture.rs` (73) Pictures floating over a sheet.
- `crates/ss-model/src/pivot.rs` (108) Pivot tables, modeled for what a reader has to know about them.
- `crates/ss-model/src/shift.rs` (433) Where a row or column ends up when rows or columns are inserted or deleted.
- `crates/ss-model/src/store.rs` (639) The sparse cell store.
- `crates/ss-model/src/strings.rs` (141) String interning for cell text.
- `crates/ss-model/src/style.rs` (1191) Cell formatting.
- `crates/ss-model/src/table.rs` (361) Tables — `xl/tables/tableN.xml` — and the look a table style gives a cell.
- `crates/ss-model/src/workbook.rs` (1211) Sheets and workbooks.

### `ss-xls`

- `crates/ss-xls/src/error.rs` (74) Errors from reading a legacy workbook.
- `crates/ss-xls/src/formula.rs` (552) Parsed expressions: BIFF's RPN token stream back into formula text.
- `crates/ss-xls/src/func.rs` (287) The built-in function table: BIFF stores a function as a number.
- `crates/ss-xls/src/globals.rs` (229) The workbook globals substream: everything shared between the sheets.
- `crates/ss-xls/src/lib.rs` (103) Reading legacy `.xls` workbooks — Excel 97 through 2003 — into the [`ss_model`] document model.
- `crates/ss-xls/src/record.rs` (149) The BIFF record stream: a `u16` type, a `u16` length, and that many bytes.
- `crates/ss-xls/src/sheet.rs` (467) One worksheet substream: the cells, and the geometry around them.
- `crates/ss-xls/src/string.rs` (338) BIFF8 strings, and the shared string table they mostly live in.
- `crates/ss-xls/src/style.rs` (319) `FONT`, `FORMAT`, `XF` and `PALETTE` into the model's style table.
- `crates/ss-xls/src/tests.rs` (571) Reading a workbook laid out from the specification.

### `ss-xlsx`

- `crates/ss-xlsx/src/autofilter.rs` (220) `<autoFilter>`, read on its own.
- `crates/ss-xlsx/src/comments.rs` (248) Reading and writing the notes on a sheet's cells.
- `crates/ss-xlsx/src/drawing.rs` (280) Where a chart sits: `xl/drawings/drawingN.xml`.
- `crates/ss-xlsx/src/error.rs` (74) Errors from reading a workbook.
- `crates/ss-xlsx/src/lib.rs` (617) Reading xlsx workbooks into the [`ss_model`] document model.
- `crates/ss-xlsx/src/parts.rs` (177) Locating the parts of a workbook through the relationship graph.
- `crates/ss-xlsx/src/pivot.rs` (264) Pivot table parts: the layout and the cache definition beside it.
- `crates/ss-xlsx/src/shared_strings.rs` (199) The shared string table (`sharedStrings.xml`).
- `crates/ss-xlsx/src/sheet.rs` (1546) Worksheet parts: the cell grid, and the geometry around it.
- `crates/ss-xlsx/src/styles.rs` (628) The style table (`styles.xml`).
- `crates/ss-xlsx/src/table.rs` (155) `xl/tables/tableN.xml` — a table's range, its style name, and its emphases.
- `crates/ss-xlsx/src/theme.rs` (146) The document theme's colour scheme.
- `crates/ss-xlsx/src/workbook_part.rs` (231) `workbook.xml` — the sheet list and the defined names.
- `crates/ss-xlsx/src/write/blank.rs` (191) Authoring a package for a workbook that has never been in a file.
- `crates/ss-xlsx/src/write/cells.rs` (123) What a cell holds, in terms both sides of a save can be compared in.
- `crates/ss-xlsx/src/write/chart_out.rs` (1131) Retitling a chart without disturbing anything else in it.
- `crates/ss-xlsx/src/write/drawing_out.rs` (328) Moving, resizing and removing anchored pictures in `xl/drawings/drawingN.xml`.
- `crates/ss-xlsx/src/write/insert.rs` (986) Putting a chart or a picture into a package that has never held one.
- `crates/ss-xlsx/src/write/mod.rs` (454) Writing a workbook back out.
- `crates/ss-xlsx/src/write/sheet_out.rs` (3406) Writing a worksheet by editing the one that was read.
- `crates/ss-xlsx/src/write/splice.rs` (310) Reading a part while keeping the bytes it was read from.
- `crates/ss-xlsx/src/write/strings_out.rs` (235) The shared string table, as a writer sees it.
- `crates/ss-xlsx/src/write/styles_out.rs` (953) Adding to `styles.xml` without rewriting it.
- `crates/ss-xlsx/src/write/workbook_out.rs` (361) Rewriting the sheet list in `workbook.xml`.
- `crates/ss-xlsx/src/xml.rs` (269) Low-level XML helpers shared by the part readers.

### `ui-kit`

- `crates/ui-kit/src/brand.rs` (31) The window icon, as the windowing system takes it.
- `crates/ui-kit/src/catalogue.rs` (504) What type the machine actually has, by the name a document calls it.
- `crates/ui-kit/src/chart.rs` (310) Drawing a chart on the screen.
- `crates/ui-kit/src/chooser.rs` (141) The operating system's file chooser, asked without stopping the window.
- `crates/ui-kit/src/dialog.rs` (804) The look of every dialog both apps put on the screen.
- `crates/ui-kit/src/drive/painted.rs` (255) What a frame painted, asked about the way a test asks.
- `crates/ui-kit/src/drive.rs` (1202) Driving an application by keystroke, without a window.
- `crates/ui-kit/src/fonts.rs` (1431) Real type faces, loaded from the system at startup.
- `crates/ui-kit/src/headless.rs` (54) An application running without a person in front of it: under a test.
- `crates/ui-kit/src/keys.rs` (105) Keyboard shortcuts, matched on every modifier.
- `crates/ui-kit/src/lib.rs` (46) Shared application shell for Calx and Scriva.
- `crates/ui-kit/src/menu.rs` (1094) A menu bar, and menus that look like menus.
- `crates/ui-kit/src/paths.rs` (99) Config and state directory resolution.
- `crates/ui-kit/src/recent.rs` (205) The files that have been opened, most recent first.
- `crates/ui-kit/src/scroll.rs` (65) Scroll bars in the suite's palette.
- `crates/ui-kit/src/shell.rs` (654) The window shell both apps boot into.
- `crates/ui-kit/src/theme.rs` (187) The design language, as named numbers.

### `wp-compare`

- `crates/wp-compare/src/diff.rs` (1319) Matching two readings of the same page against each other.
- `crates/wp-compare/src/main.rs` (929) Where Scriva's page differs from Word's, ranked.
- `crates/wp-compare/src/marks.rs` (614) The ink on a page that is not type, and whether the two renderings agree about where it went.
- `crates/wp-compare/src/ours.rs` (558) Where Scriva put every word of a document.
- `crates/wp-compare/src/reference.rs` (755) Where the reference application put every mark of the same document.

### `wp-doc`

- `crates/wp-doc/src/art.rs` (709) OfficeArt: the drawing layer a `.doc` keeps beside its text.
- `crates/wp-doc/src/fib.rs` (257) The File Information Block: where everything else in a `.doc` is.
- `crates/wp-doc/src/fkp.rs` (241) The bin tables: how a `.doc` says which formatting applies where.
- `crates/wp-doc/src/font.rs` (137) The font table: what a `ftc` is the name of.
- `crates/wp-doc/src/lib.rs` (194) Reading Word 97–2003 `.doc` files.
- `crates/wp-doc/src/list.rs` (406) The lists a `.doc` numbers its paragraphs from.
- `crates/wp-doc/src/picture.rs` (162) Inline pictures: the ones that sit in a line of text like a very large character.
- `crates/wp-doc/src/piece.rs` (299) The piece table: where each character actually is.
- `crates/wp-doc/src/section.rs` (151) Page setup: the paper the document was written on.
- `crates/wp-doc/src/sprm.rs` (1079) Sprms: the property opcodes a `.doc` states its formatting in.
- `crates/wp-doc/src/style.rs` (289) The stylesheet: what the style indices in the rest of the file mean.
- `crates/wp-doc/src/text.rs` (1467) Turning a `.doc` into the document model the rest of the application uses.

### `wp-docx`

- `crates/wp-docx/src/bits.rs` (309) The small parts: `settings.xml`, `theme1.xml`, and `people.xml`.
- `crates/wp-docx/src/body.rs` (1340) Reading a body of blocks: `<w:body>`, a table cell, a header, a footnote, a comment, or the contents of a content control.
- `crates/wp-docx/src/ctx.rs` (165) What a part reader needs beyond the bytes in front of it.
- `crates/wp-docx/src/error.rs` (53) What can go wrong reading or writing a `.docx`.
- `crates/wp-docx/src/fonts.rs` (244) `fontTable.xml` — the faces a document carries with it.
- `crates/wp-docx/src/lib.rs` (256) `.docx` reader and writer, over `ooxml` and `wp-model`.
- `crates/wp-docx/src/link.rs` (66) Relating a hyperlink to where it goes, which in a `.docx` is not where the link is.
- `crates/wp-docx/src/media.rs` (207) Putting an image *into* a package, which is three things and not one.
- `crates/wp-docx/src/notes.rs` (198) `footnotes.xml`, `endnotes.xml`, `comments.xml` and `commentsExtended.xml`.
- `crates/wp-docx/src/numbering.rs` (467) `numbering.xml` — abstract list definitions and the instances of them.
- `crates/wp-docx/src/parts.rs` (271) Locating a document's parts through the relationship graph.
- `crates/wp-docx/src/pict.rs` (469) `<w:pict>`: the VML a watermark is still written as.
- `crates/wp-docx/src/props.rs` (956) Reading `<w:rPr>`, `<w:pPr>` and `<w:sectPr>`.
- `crates/wp-docx/src/styles.rs` (350) `styles.xml` — document defaults and every named style.
- `crates/wp-docx/src/write/blank.rs` (398) Authoring a package for a document that has never been in a file.
- `crates/wp-docx/src/write/comments_out.rs` (371) `comments.xml` and `commentsExtended.xml`: rewritten when the comments changed, authored when the package has none.
- `crates/wp-docx/src/write/drawing.rs` (567) Putting a drawing back, with the two things a user can change to it applied.
- `crates/wp-docx/src/write/emit.rs` (1448) Serializing the model back to WordprocessingML.
- `crates/wp-docx/src/write/headers_out.rs` (315) Header and footer parts: rewritten when edited, authored when new.
- `crates/wp-docx/src/write/mod.rs` (835) Writing a document back to its package.
- `crates/wp-docx/src/write/notes_out.rs` (294) Footnote and endnote parts, authored for a package that has none.
- `crates/wp-docx/src/write/numbering_out.rs` (413) The numbering part: new definitions appended, existing bytes untouched.
- `crates/wp-docx/src/write/splice.rs` (222) Reading a part while keeping the bytes it was read from.
- `crates/wp-docx/src/xml.rs` (236) Low-level XML helpers.

### `wp-layout`

- `crates/wp-layout/src/block.rs` (6454) Blocks, tables and pages: turning a document into a stack of paper.
- `crates/wp-layout/src/field.rs` (332) What a field evaluates to, and the two passes that make a page number right.
- `crates/wp-layout/src/inline.rs` (3869) Laying a paragraph out into lines.
- `crates/wp-layout/src/lib.rs` (42) Text layout: shaping, line breaking, and pagination.
- `crates/wp-layout/src/linebreak.rs` (281) Where a line may be broken.
- `crates/wp-layout/src/memo.rs` (276) The lines of the paragraphs that did not change.
- `crates/wp-layout/src/notes.rs` (141) What stands in the text where a note is referenced, and what the note costs.
- `crates/wp-layout/src/resolve.rs` (365) From resolved properties to something a renderer can draw with.
- `crates/wp-layout/src/shape.rs` (246) What the layout engine needs from a font, and nothing more.

### `wp-model`

- `crates/wp-model/src/banding.rs` (355) A table style is a scheme, not a set of properties.
- `crates/wp-model/src/color.rs` (500) Colours, and the three ways a Word document writes one down.
- `crates/wp-model/src/doc.rs` (2110) The document tree: body, blocks, paragraphs, runs, and the pieces inside a run.
- `crates/wp-model/src/field.rs` (246) Field codes: the little programs embedded in a document's text.
- `crates/wp-model/src/lib.rs` (61) Word document model: paragraph/run tree, styles, sections, numbering, tables.
- `crates/wp-model/src/numbering.rs` (994) Numbering: bullets, numbered lists, and the nine levels of both.
- `crates/wp-model/src/outline.rs` (588) The document's skeleton: headings, bookmarks, and the table of contents built from them.
- `crates/wp-model/src/prop.rs` (1318) Run and paragraph properties — `<w:rPr>` and `<w:pPr>`.
- `crates/wp-model/src/revision.rs` (323) Tracked changes, comments, and bookmarks.
- `crates/wp-model/src/section.rs` (753) Sections: page size, margins, columns, and the headers and footers that belong to them.
- `crates/wp-model/src/style.rs` (989) Styles, and the layers a paragraph's appearance is assembled from.
- `crates/wp-model/src/table.rs` (1096) Tables: a grid of columns, a list of rows, and two different ways a cell can cover more than one square of it.
- `crates/wp-model/src/units.rs` (370) The five units a Word document measures in.

### `wp-odf`

- `crates/wp-odf/src/container.rs` (449) The zip an OpenDocument file is, and everything in it.
- `crates/wp-odf/src/content.rs` (780) `content.xml` and `styles.xml` — the two parts that between them are the document.
- `crates/wp-odf/src/draw.rs` (609) `<draw:frame>` — the box a picture is in.
- `crates/wp-odf/src/fonts.rs` (210) `<office:font-face-decls>` — the faces a document names, and the ones it carries.
- `crates/wp-odf/src/lib.rs` (361) Reading OpenDocument text documents — `.odt`.
- `crates/wp-odf/src/list.rs` (424) `<text:list-style>` and `<text:outline-style>` — how a document numbers things.
- `crates/wp-odf/src/manifest.rs` (226) `META-INF/manifest.xml` — what an ODF package says is in it.
- `crates/wp-odf/src/page.rs` (313) `<style:page-layout>` and `<style:master-page>` — the paper, and what is printed on every sheet of it.
- `crates/wp-odf/src/props.rs` (941) `<style:*-properties>` — everything a style says, in the vocabulary it says it in.
- `crates/wp-odf/src/styles.rs` (442) `<office:styles>`, `<office:automatic-styles>` and `<style:default-style>`.
- `crates/wp-odf/src/table.rs` (331) `<table:table>` and what is in it.
- `crates/wp-odf/src/write/auto.rs` (812) Automatic styles, and the minting of one for formatting a person applied by hand.
- `crates/wp-odf/src/write/blank.rs` (577) Authoring a package for a document that has never been in one.
- `crates/wp-odf/src/write/emit.rs` (948) Writing a paragraph the way OpenDocument writes one.
- `crates/wp-odf/src/write/media.rs` (75) Putting an image *into* an OpenDocument package, which is one thing where a `.docx` makes it three.
- `crates/wp-odf/src/write/mod.rs` (1184) Writing a document back to its package.
- `crates/wp-odf/src/write/splice.rs` (196) Reading `content.xml` while keeping the bytes each event came from.
- `crates/wp-odf/src/xml.rs` (256) Low-level XML helpers, the twin of `wp_docx::xml`.

### `wp-print`

- `crates/wp-print/src/lib.rs` (44) Putting laid-out pages on paper: a PDF for a file, GDI for a printer.
- `crates/wp-print/src/ops.rs` (1016) One page flattened into draw operations no device has opinions about.
- `crates/wp-print/src/pdf.rs` (1072) Laid-out pages to a PDF, with the document's own fonts inside it.
- `crates/wp-print/src/ttf.rs` (454) The little of TrueType a PDF embedder has to read.
- `crates/wp-print/src/win.rs` (623) The Windows printer, through the dialog everyone knows.

### `wp-text`

- `crates/wp-text/src/encoding.rs` (339) Deciding what bytes a text file is written in.
- `crates/wp-text/src/lib.rs` (14) Plain text and Markdown, read and written.
- `crates/wp-text/src/markdown.rs` (595) Markdown, both ways.

## Measured

Every comment that says an application was *measured* — the rules this code keeps because Word, Excel or LibreOffice was asked, not because they were reasoned out. One line each, cut at the sentence; the rest of the comment is at the place. A sentence counts when it says "measured" and names what was asked, so a rule written without both is missing here: write both, and it will be listed.

### `scriva`

- `crates/app-scriva/src/app/bands.rs:224` `content` is what goes in it: nothing for a band being made from scratch, and a copy of the inherited one for a section being unlinked — which is what Word does, measured: unlink a section's header and the words stay …
- `crates/app-scriva/src/app/bands.rs:349` **Breaking it copies rather than empties.** Word's own answer, measured over COM: unlink a second section's header and the words are still there, while the first section keeps a copy of its own — so the two can then b…
- `crates/app-scriva/src/app/tables.rs:460` Measured: a cell that states none is laid to its *content* by Word, whatever the grid says — a 468pt table came back 28pt wide, its second column as wide as "B1", and an empty column under a point.
- `crates/app-scriva/src/app/watermark.rs:272` measured against a watermark Word wrote itself, which put the shape in the default header and left the other two parts empty.
- `crates/app-scriva/src/app.rs:470` Word's own Page Setup keeps those last two on the same sheet, under "From edge", and they belong with the margins because they are measured against the same four edges.
- `crates/app-scriva/src/app.rs:4717` 96 pixels to the inch: what a screen snippet is measured in, and what Word assumes of an image that does not say otherwise.
- `crates/app-scriva/src/app.rs:4851` What a new document is in Word today, measured on the reference machine (Word 16.0.20326, 2026-09-13): twelve points, eight points after every paragraph, and a line of 278 to 240 — the document defaults Word writes in…
- `crates/app-scriva/src/author.rs:5` Nothing measured what it *writes* — and the faults that cost a week of afternoons were all there: a new document that stated no defaults and came back from Word a third taller, an inserted table whose cells stated no …
- `crates/app-scriva/src/edit.rs:677` Measured on Word, not designed: a page break *inside* a cell is nothing to Word's layout, wherever in the cell it is, and the layout here ignores one too.
- `crates/app-scriva/src/edit.rs:2026` Measured on Word 16 (`bugs/page-break-in-table-cell.md` in the story):
- `crates/app-scriva/src/shaper.rs:454` A face this machine does not have, whose line Word was measured to lay: its ideal, and the base every face without a measured base gets — the ideal to a twenty-fourth of a point.
- `crates/app-scriva/src/shaper.rs:565` Word's laid line pitch, measured rather than derived.
- `crates/app-scriva/src/shaper.rs:576` **Aptos is measured and deliberately absent.** Word's default face since 2024 fits the same law with a base of exactly 1.2 times the size and a correction of *six* tenths of a point — `tools/probe` writes the probes a…

### `chart`

- `crates/chart/src/draw.rs:32` This and the three below are Word's, measured from charts Word rendered itself: probe documents whose chart area carries a visible border, so that the plot rectangle could be read against the chart's own edges rather …
- `crates/chart/src/draw.rs:162` A line series that says nothing about smoothing is smoothed: Excel's reading, measured against a part that states none.
- `crates/chart/src/draw.rs:1006` The label's em box centred in the footer band, which is where Word sets them — measured ink margins of 9.3 and 9.1 in the 24.8pt band.
- `crates/chart/src/draw.rs:1051` The rule, measured against what Excel and Word draw: the major unit is the "nice" number (1, 2 or 5 times a power of ten) nearest a fifth of the range, and the ends are the data padded by 5% and pushed out to the next…
- `crates/chart/src/draw.rs:2308` Measured from charts Word rendered itself: the plot stands 1.74 label-sizes clear of the widest value label on one side and of the legend on the other, with a margin of 1.2 above and down the right and 2.45 below for …
- `crates/chart/src/model.rs:172` `None` when the part says nothing, which Excel reads as *smoothed* for a line chart — the one boolean in the part whose silence means yes (measured 2026-08-21).
- `crates/chart/src/model.rs:353` Excel's own Insert writes 100 on every stack, which is why nobody sees the default until a second producer leaves it out (measured 2026-08-22).
- `crates/chart/src/model.rs:373` Excel inserts 75 and draws a part that states none with *no* hole — the schema's ten is not what it does (measured 2026-08-21).

### `ss-formula`

- `crates/ss-formula/src/sort.rs:363` **Excel's guess, measured, and no cleverer.** Excel 16 was asked, through `Range.Sort` and `Range.RemoveDuplicates` with `Header:=xlGuess` — the ribbon's own guess — what it made of five columns.
- `crates/ss-formula/src/sort.rs:819` Measured on Excel 16: `topic, beta, alpha, gamma` sorts whole — the earlier rule here kept `topic` back, on the argument that a heading sorted into the data is the more visible mistake, and it was not what Excel does …

### `ss-model`

- `crates/ss-model/src/table.rs:14` Excel's own definitions are not published in the package and could not be measured here, so the colours below are our rendering of the gallery rather than a copy of it — the same kind of approximation as drawing a 3-D…

### `ss-xls`

- `crates/ss-xls/src/sheet.rs:423` Storing the automatic ones would freeze every row at whatever the version of Excel that wrote the file measured its default font to be.

### `ss-xlsx`

- `crates/ss-xlsx/src/sheet.rs:753` Recording auto-fit heights would freeze every row at whatever the producing version of Excel measured.
- `crates/ss-xlsx/src/write/insert.rs:510` Excel reads a line series that says nothing about smoothing as smoothed (measured 2026-08-21): a chart of straight lines must say so, and a scatter's straight lines likewise.

### `ui-kit`

- `crates/ui-kit/src/fonts.rs:280` Not guessed: measured against Word 16 laying out the LibreOffice sample documents on this machine (2026-08-15).
- `crates/ui-kit/src/fonts.rs:383` A face Word was measured to lay, drawn in the generic face of its shape but laid at the missing face's own pitch — see [`measured_pitch`] — so lines break where Word breaks them, in letters that are not quite it.
- `crates/ui-kit/src/fonts.rs:391` What Word was measured to lay a face in, for a machine without the face.
- `crates/ui-kit/src/fonts.rs:668` A machine can therefore *have* the face Word laid a document in while every lookup by file name says it does not, and the document is then measured in a stand-in whose every line breaks somewhere else.

### `wp-compare`

- `crates/wp-compare/src/main.rs:39` Three further things are deliberately left out, each for a measured reason rather than a preference — a leadered tab's dots, which neither renderer chose the number of; a shape's own words, which Word draws into a PDF…
- `crates/wp-compare/src/reference.rs:13` LibreOffice is the implementation ODF is defined against in practice, and Word reads `.odt` through a converter it wrote for a format it does not own — so an `.odt` is measured against LibreOffice.
- `crates/wp-compare/src/reference.rs:18` `Range.Information(5|6)` answers to a twentieth of a point, but it costs Word a layout pass per call — measured here at about 110ms, per word — so a sixteen-page document is hours.
- `crates/wp-compare/src/reference.rs:55` a `.docx` measured against anything but Word, or an `.odt` measured against Word's converter for a format it does not own, is a number about the wrong thing.

### `wp-doc`

- `crates/wp-doc/src/art.rs:127` **Word's own watermark is half of one**, and a reader that ignores it stamps the page twice as dark as Word does — measured, Word's grey comes out at 223 over white paper where the stated `#C0C0C0` alone would give 192.
- `crates/wp-doc/src/sprm.rs:858` Measured against Word on the demonstration document, whose headings report KeepWithNext true and KeepTogether false: read the other way, "keep these lines together" dragged the empty line a page break ends onto a page…
- `crates/wp-doc/src/text.rs:96` What Word's own converter says of a `.doc` saved as `.docx`, measured on Word 16: a Word 2003 document, laid out with that Word's rules.
- `crates/wp-doc/src/text.rs:178` Measured rather than counted off the spec's bit list: the same document saved by Word with the option on and off differs in this one bit, and Word then lays eight-point Arial on an 8.94pt line instead of a 9.20pt one.
- `crates/wp-doc/src/text.rs:191` Measured rather than counted off the spec: Word reports this document as having "don't add automatic tab stop for hanging indent" set and the corpus's own `.doc` files as not, and the byte agrees with it both ways.
- `crates/wp-doc/src/text.rs:807` Measured against Word's own export of this document: the grey that reaches the paper is 223, where the stated `#C0C0C0` on its own would put down 192.
- `crates/wp-doc/src/text.rs:1070` Measured: the demonstration document's tables state 720, Word rules them at 720 with their text a padding further in at 828, and the file says 828 itself in the `sprmTWidthIndent` alongside.
- `crates/wp-doc/src/text.rs:1156` Measured: the same document saved by Word with "don't add space between lines" on and off differs in this one bit, and with it set Word lays eight-point Arial on an 8.94pt line instead of a 9.20pt one — a quarter of a…

### `wp-docx`

- `crates/wp-docx/src/body.rs:1276` Measured in Word 16 (2026-09-13): a table stating `tblInd` 0 with cell margins of 108 has its rule on the margin as a Word 2013 document and its text on the margin — the rule 108 twips out — as a Word 2007 one.
- `crates/wp-docx/src/fonts.rs:68` Word substitutes for it instead: measured on such a document, whose list levels name a symbol face 1.48em tall, Word pitches every bulleted line at the ascent of the *text's* face and not at the symbol's, which it cou…

### `wp-layout`

- `crates/wp-layout/src/block.rs:121` **The size comes from the shape, not from the text.** WordArt has no point size — Word stretches the glyphs until they fill the box — so the face is measured once and scaled to the width it has to cover, capped so a s…
- `crates/wp-layout/src/block.rs:194` Word, measured: a WordArt shape 400 by 200 points draws "CONFIDENTIAL", "gypsy", "Hg" and "xxxx" each with their outlines spanning 400 by 200 to a fiftieth of a point — so four strings of wildly different proportions …
- `crates/wp-layout/src/block.rs:207` Measured against Word's own diagonal `CONFIDENTIAL`, a Courier New shape 609.10 by 152.25 points: the advance came out at 50.758 points, which is that face's 1229/2048 em at 84.583, and every letter's drawn outline ma…
- `crates/wp-layout/src/block.rs:470` **Word does not space a paragraph away from the top of a page it fell onto.** Measured: a paragraph set 24 points before begins at the top margin exactly when the page above it simply ran out of room, and keeps all 24…
- `crates/wp-layout/src/block.rs:910` The separator's own line draws the rule Word draws through it — three points above its baseline, measured.
- `crates/wp-layout/src/block.rs:1670` Measured, not read: Word's own printed output over `w:sz` 2 to 48 and `w:space` 0 to 20 puts the rule's inner edge exactly `space + 1.4pt` outside the text column on the sides, and `space` plus the rule's own thicknes…
- `crates/wp-layout/src/block.rs:1945` Measured from Word to the twip — thirty lines of Verdana at 12.083pt with a 12.583pt line every seventh, averaging the design height.
- `crates/wp-layout/src/block.rs:2186` The grid is a starting point rather than the answer: a table whose columns do not add up to its width is normal — Word writes the widths it last measured — so they are scaled to fit.
- `crates/wp-layout/src/block.rs:2267` Measured: Word lays the demonstration document's widest table at its grid's 478.8pt in a 468pt column, and asked over COM for its columns answers with the grid to the twip.
- `crates/wp-layout/src/block.rs:2281` A table placed by its justification is measured from the text column and not from the indent: Word centres the demonstration document's nested table on 306, the middle of the column, with no sign of the hang that `w:t…
- `crates/wp-layout/src/block.rs:2357` Measured against the two-by-two nested table of the demonstration document, whose merged cell holds two paragraphs: Word puts the second beside the second row's own text, where charging the first row for both put it a…
- `crates/wp-layout/src/block.rs:2452` The paragraph a cell must end with after a table is punctuation, not a line: the format forbids a cell ending in a table, so Word writes an empty paragraph there and gives it no height whatever — measured, its spacing…
- `crates/wp-layout/src/block.rs:3074` Measured against Word on the demonstration document: a table whose heading row very nearly fitted at the foot of a page had all of its words placed there and one and nine tenths of a point of padding sent over, where …
- `crates/wp-layout/src/block.rs:3572` **Where the boundary really lies is not measured.** The evidence brackets it and no more: at −18 points of leftover measure Word sets the text below, and at +290 it sets it beside.
- `crates/wp-layout/src/block.rs:3798` Measured from Word: a 2pt-bordered table shifts its text down 2pt exactly.
- `crates/wp-layout/src/block.rs:3988` Measured on a heading style that pairs `<w:pageBreakBefore/>` with 24 points before: Word sets it 24 points down every page it opens, and a layout that dropped them stood the whole page and every page after it that fa…
- `crates/wp-layout/src/block.rs:4089` Word, measured: a paragraph set twenty-four points before begins at the top margin exactly when the page above it ran out of room, keeps all twenty-four when the writer typed the break himself, and keeps them again at…
- `crates/wp-layout/src/block.rs:4123` Word, measured on `table_render_test.doc`: a heading row that very nearly fitted at the foot of page five moved whole to page six, where this had placed all of its words and sent one and nine tenths of a point of padd…
- `crates/wp-layout/src/block.rs:4250` Measured on Word 16: a page break inside a table cell is nothing to the layout, whether mid-paragraph, alone in its paragraph or as `pageBreakBefore` on a cell's paragraph.
- `crates/wp-layout/src/block.rs:4798` Measured against Word on the demonstration document's letterhead:
- `crates/wp-layout/src/block.rs:5295` Measured against a two-section document Word itself wrote and this application read back.
- `crates/wp-layout/src/block.rs:6059` Word's own watermark on the document this was measured against is Courier New filling 609 points of width in a box 152 points tall, and its letters stand half again as tall as that width alone would make them.
- `crates/wp-layout/src/block.rs:6132` Word, measured: a WordArt shape 400 by 200 points draws "CONFIDENTIAL", "gypsy", "Hg" and "xxxx" with their *outlines* spanning 400 by 200 in every case — so the box that fills the shape is the ink's, and a face's asc…
- `crates/wp-layout/src/block.rs:6201` Word, measured on `table_render_test.doc`: a Courier New shape 609.10 by 152.25 points draws `CONFIDENTIAL` with an advance of 50.758 points a letter, which is that face's em at 84.583, and with every letter's outline…
- `crates/wp-layout/src/inline.rs:209` Measured against Word.
- `crates/wp-layout/src/inline.rs:1245` A page or column break inside a table cell is nothing to Word — measured: no new page, and the text either side of it runs on together, not even a line apart.
- `crates/wp-layout/src/inline.rs:1523` **Word closes the spaces of a justified line up rather than letting a word it could nearly fit fall to the next line.** Measured against Word over a paragraph of fixed text and a right indent stepped a tenth of a poin…
- `crates/wp-layout/src/inline.rs:1607` Measured against Word on a list indented to 1600 twips whose label ends at 1400: the text lands on the 1600 and not on the default stop at 1440 that stands between them.
- `crates/wp-layout/src/inline.rs:1954` Word, measured over sixteen mixtures of Arial, Verdana, Georgia, Calibri, Times New Roman and Courier New at 8, 10, 11 and 16 points:
- `crates/wp-layout/src/inline.rs:2005` Measured: Word draws an eight-point entry on an eight-point line with a twenty-two point tab in the middle of it.
- `crates/wp-layout/src/inline.rs:2017` Measured: Word's own figure paragraph is 298.2 points tall for a 293.05 point picture, and the five that are left is one line of the mark's own type.
- `crates/wp-layout/src/inline.rs:2026` **A list's label raises a line but does not deepen it.** Word, measured: a bulleted Arial 11 line pitches at 13.39, which is Symbol's ascent — the bullet is drawn in Symbol whatever the words are in — plus *Arial's* d…
- `crates/wp-layout/src/inline.rs:2049` Measured against Word over the eleven inline figures of the demonstration document, every one of them a picture alone on its line: each paragraph came out exactly as tall as its picture, to the six tenths of a point W…
- `crates/wp-layout/src/inline.rs:2134` Measured on the floating-image document, whose 90pt picture shares a 1.158-spaced line of 12pt type: Word pitches that line at 95.3, not at the 93.0 the type's own descent alone gives, and every line after it was two …
- `crates/wp-layout/src/inline.rs:2156` Word seats the baseline at the face's ascent plus its line gap and leaves it there: measured over Arial, Verdana and Georgia at line multiples of 1.0, 1.15, 1.5 and 2.0, the first baseline of a spaced paragraph never …
- `crates/wp-layout/src/inline.rs:2190` What Word does with the two channels of such a line has not been measured; spreading them is the one answer that is certainly wrong.
- `crates/wp-layout/src/inline.rs:2587` Measured against Word: giving that style a loud colour changes every other run wearing it and changes nothing in the contents, while swapping those same runs to another character style takes effect at once.
- `crates/wp-layout/src/inline.rs:3278` Word, measured over sixteen mixtures of six faces: an Arial 11 line with one Courier New letter on it pitches at 13.61, which is taller than Arial's own 12.66 *and* taller than Courier's own 12.47 — Arial's ascent ove…
- `crates/wp-layout/src/inline.rs:3329` Word, measured: a bulleted Arial 11 line pitches at 13.39 — Symbol's ascent, because the bullet is drawn in Symbol whatever the words are in, plus *Arial's* descent.
- `crates/wp-layout/src/inline.rs:3369` Measured against Word on probes: a paragraph mark carrying `<w:b/><w:u/>` (LibreOffice copies the run's formatting onto the mark) draws the *number* bold but not underlined, and the tab after it stays bare too.
- `crates/wp-layout/src/inline.rs:3793` Word, measured over the eleven inline figures of the demonstration document: a paragraph holding nothing but a picture comes out exactly as tall as the picture, to the six tenths of a point Word reports a position in.
- `crates/wp-layout/src/resolve.rs:47` Measured against Word over `w:sz` 2 to 24 and `w:space` 0 and 4: the line grows by the gap plus the rule's thickness above and below, and the run is pushed the same distance right of where it would have sat.

### `wp-model`

- `crates/wp-model/src/doc.rs:850` Unstretched, the words are set at the size whose advances fill the shape's width and are only scaled down the page until one em covers its height — which for Word's own diagonal `CONFIDENTIAL`, measured, is Courier Ne…
- `crates/wp-model/src/doc.rs:889` Measured against Word itself, which bakes the answer into its own PDF: a ramp of every grey from 0 to 255, exported at seven settings of brightness and contrast, gives
- `crates/wp-model/src/doc.rs:922` The two notations say the same thing, which is not obvious and was measured rather than assumed: handed a shape stating `gain="19661f" blacklevel="22938f"`, Word reports the picture's brightness as 0.85 and its contra…
- `crates/wp-model/src/doc.rs:1208` **Fifteen is where Word began closing up the spaces of a justified line.** Measured against Word over a paragraph of fixed text and a right indent stepped a tenth of a point at a time: a document in mode fifteen holds…
- `crates/wp-model/src/doc.rs:1238` **The two formats answer this differently and neither states it.** Measured on `word-odf-export.odt`, whose body sets `Body Text` an eighth of an inch above and below and `Heading 2` a quarter above: LibreOffice puts …
- `crates/wp-model/src/style.rs:373` Nothing in ECMA-376 says so; it was measured, by asking Word for the resolved size of one cell across twenty-three variants of one document (2026-08-23):
- `crates/wp-model/src/style.rs:497` Measured against a second producer's tables, whose styles pad every cell by 115 twips and whose tables state an indent of -7: Word rules them seven twips into the margin and sets their text a padding further in, so an…
- `crates/wp-model/src/table.rs:535` Measured: this is what Word's Insert Below copies, with empty cells (LEARNINGS.md, "A column Word inserts…").
- `crates/wp-model/src/table.rs:589` Measured on Word 16: the new column takes the width of the column to its right, or of the last column when it is appended, and every other column keeps its width — the table grows by the new one (LEARNINGS.md, "A colu…
- `crates/wp-model/src/table.rs:975` Word 16, measured: 2in, 4in, 1in; a column right of the 4in one is 1in, one left of the 2in one is 2in, one after the last copies it.

### `wp-odf`

- `crates/wp-odf/src/lib.rs:317` The flag the layout engine reads for justified spacing is left where it is, which means the oldest behaviour — the right answer here, because the rule it gates was measured out of Word and no clause of ODF asks for it.

### `wp-print`

- `crates/wp-print/src/ops.rs:92` How long the rule above a page's footnotes is: two inches, measured off Word's own output.
- `crates/wp-print/src/ttf.rs:17` WordArt is fitted to a shape by its *path* rather than by its line box — Word, measured — and no other table says how tall a drawn letter is.

## Menus

Every row of every menu, and the keys that choose it from the document — walked by `no_two_rows_of_a_menu_share_a_letter` in each application, which also fails if two rows of one menu share a letter. Rows without a letter are reached by the arrows or the pointer.

### Scriva

| keys | row |
|---|---|
| `Alt+A, I` | Insert ▸ |
| `Alt+A, D` | Delete ▸ |
| `Alt+A, G` | Merge Cells |
| `Alt+A, B` | Borders ▸ |
| `Alt+A, C` | Border Colour ▸ |
| `Alt+A, S` | Shading ▸ |
| `Alt+A, W` | Column Width… |
| `Alt+A, M` | Cell Margins… |
| `Alt+A, B, A` | All |
| `Alt+A, B, N` | None |
| `Alt+A, C, A` | Automatic |
| `Alt+A, C, O` | Other… |
| `Alt+A, D, R` | Row |
| `Alt+A, D, C` | Column |
| `Alt+A, D, T` | Table |
| `Alt+A, I, A` | Row Above |
| `Alt+A, I, B` | Row Below |
| `Alt+A, I, L` | Column Left |
| `Alt+A, I, R` | Column Right |
| `Alt+A, S, N` | No Fill |
| `Alt+E, U` | Undo |
| `Alt+E, R` | Redo *(disabled here)* |
| `Alt+E, T` | Cut *(disabled here)* |
| `Alt+E, C` | Copy *(disabled here)* |
| `Alt+E, P` | Paste |
| `Alt+E, N` | Paste Unformatted |
| `Alt+E, F` | Find… |
| `Alt+E, E` | Replace… |
| `Alt+E, G` | Go To… |
| `Alt+E, A` | Select All |
| `Alt+F, N` | New |
| `Alt+F, O` | Open… |
| `Alt+F, R` | Recent ▸ |
| `Alt+F, S` | Save |
| `Alt+F, A` | Save As… |
| `Alt+F, P` | Print… |
| `Alt+F, D` | Export as PDF… |
| `Alt+F, C` | Close |
| `Alt+F, X` | Exit |
| `Alt+F, R, 1` | 1 walked.docx |
| `Alt+F, R, L` | Clear List |
| `Alt+H, K` | Keyboard Shortcuts… |
| `Alt+H, U` | User Guide |
| `Alt+H, A` | About Scriva |
| `Alt+I, P` | Picture… |
| `Alt+I, T` | Table ▸ |
| `Alt+I, B` | Page Break |
| `Alt+I, N` | Page Number ▸ |
| `Alt+I, H` | Header ▸ |
| `Alt+I, F` | Footer ▸ |
| `Alt+I, C` | Comment |
| `Alt+I, U` | Update Table of Contents |
| `Alt+I, F, E` | Edit Footer |
| `Alt+I, F, R` | Remove Footer *(disabled here)* |
| `Alt+I, H, E` | Edit Header |
| `Alt+I, H, R` | Remove Header *(disabled here)* |
| `Alt+I, N, P` | Plain Number |
| `Alt+I, N, X` | Page X of Y |
| `Alt+I, T, I` | Insert Table… |
| `Alt+L, P` | Page Setup… |
| `Alt+L, M` | Margins ▸ |
| `Alt+L, O` | Orientation ▸ |
| `Alt+L, S` | Size ▸ |
| `Alt+L, M, N` | Normal — 1" all round |
| `Alt+L, M, A` | Narrow — ½" all round |
| `Alt+L, M, O` | Moderate — 1" × ¾" |
| `Alt+L, M, W` | Wide — 1" × 2" |
| `Alt+L, M, C` | Custom Margins… |
| `Alt+L, O, P` | Portrait |
| `Alt+L, O, L` | Landscape |
| `Alt+L, S, L` | Letter — 8.5" × 11" |
| `Alt+L, S, G` | Legal — 8.5" × 14" |
| `Alt+L, S, A` | A4 — 210 × 297 mm |
| `Alt+O, N` | Font… |
| `Alt+O, B` | Bold |
| `Alt+O, I` | Italic |
| `Alt+O, U` | Underline |
| `Alt+O, T` | Strikethrough |
| `Alt+O, P` | Superscript |
| `Alt+O, R` | Subscript |
| `Alt+O, O` | Text Colour ▸ |
| `Alt+O, L` | Highlight ▸ |
| `Alt+O, F` | Font ▸ |
| `Alt+O, S` | Size ▸ |
| `Alt+O, G` | Grow |
| `Alt+O, H` | Shrink |
| `Alt+O, C` | Clear Formatting |
| `Alt+O, W` | Watermark… |
| `Alt+O, Z` | Picture Size… *(disabled here)* |
| `Alt+O, F, —` | Arial |
| `Alt+O, F, —` | Arial Narrow |
| `Alt+O, F, —` | Book Antiqua |
| `Alt+O, F, —` | Bookman Old Style |
| `Alt+O, F, —` | Calibri |
| `Alt+O, F, —` | Candara |
| `Alt+O, F, —` | Century Gothic |
| `Alt+O, F, —` | Comic Sans MS |
| `Alt+O, F, —` | Constantia |
| `Alt+O, F, —` | Corbel |
| `Alt+O, F, —` | Courier New |
| `Alt+O, F, —` | DejaVu Sans |
| `Alt+O, F, —` | Franklin Gothic Medium |
| `Alt+O, F, —` | Garamond |
| `Alt+O, F, —` | Georgia |
| `Alt+O, F, —` | Impact |
| `Alt+O, F, —` | Liberation Sans |
| `Alt+O, F, —` | Liberation Serif |
| `Alt+O, F, —` | Lucida Console |
| `Alt+O, F, —` | Lucida Sans Unicode |
| `Alt+O, F, —` | Open Sans |
| `Alt+O, F, —` | Palatino Linotype |
| `Alt+O, F, —` | Segoe UI |
| `Alt+O, F, —` | Tahoma |
| `Alt+O, F, —` | Times New Roman |
| `Alt+O, F, —` | Trebuchet MS |
| `Alt+O, F, —` | Verdana |
| `Alt+O, L, N` | None |
| `Alt+O, O, A` | Automatic |
| `Alt+O, O, M` | More Colours… |
| `Alt+O, S, —` | 8 |
| `Alt+O, S, —` | 9 |
| `Alt+O, S, —` | 10 |
| `Alt+O, S, —` | 10.5 |
| `Alt+O, S, —` | 11 |
| `Alt+O, S, —` | 12 |
| `Alt+O, S, —` | 14 |
| `Alt+O, S, —` | 16 |
| `Alt+O, S, —` | 18 |
| `Alt+O, S, —` | 20 |
| `Alt+O, S, —` | 22 |
| `Alt+O, S, —` | 24 |
| `Alt+O, S, —` | 28 |
| `Alt+O, S, —` | 32 |
| `Alt+O, S, —` | 36 |
| `Alt+O, S, —` | 48 |
| `Alt+O, S, —` | 72 |
| `Alt+P, B` | Bullets |
| `Alt+P, N` | Numbering |
| `Alt+P, L` | Align Left |
| `Alt+P, C` | Centre |
| `Alt+P, R` | Align Right |
| `Alt+P, J` | Justify |
| `Alt+P, S` | Line Spacing ▸ |
| `Alt+P, I` | Increase Indent |
| `Alt+P, D` | Decrease Indent |
| `Alt+P, P` | Paragraph… |
| `Alt+P, S, S` | Single |
| `Alt+P, S, 1` | 1.5 Lines |
| `Alt+P, S, D` | Double |
| `Alt+R, T` | Track Changes |
| `Alt+R, A` | Accept |
| `Alt+R, R` | Reject |
| `Alt+R, L` | Accept All |
| `Alt+R, J` | Reject All |
| `Alt+R, N` | Next Change |
| `Alt+R, P` | Previous Change |
| `Alt+R, C` | New Comment |
| `Alt+R, Y` | Reply to Comment |
| `Alt+R, S` | Resolve Comment |
| `Alt+R, D` | Delete Comment |
| `Alt+S, —` | Normal |
| `Alt+S, —` | Heading 1 |
| `Alt+S, —` | Heading 2 |
| `Alt+S, —` | Heading 3 |
| `Alt+S, —` | Title |
| `Alt+V, Z` | Zoom ▸ |
| `Alt+V, H` | Header and Footer |
| `Alt+V, M` | Formatting Marks |
| `Alt+V, C` | Tracked Changes |
| `Alt+V, O` | Comments |
| `Alt+V, N` | Navigation Pane |
| `Alt+V, V` | Reviewing Pane |
| `Alt+V, Z, 5` | 50% |
| `Alt+V, Z, 7` | 75% |
| `Alt+V, Z, 1` | 100% |
| `Alt+V, Z, 2` | 125% |
| `Alt+V, Z, —` | 150% |
| `Alt+V, Z, 0` | 200% |
| `Alt+V, Z, W` | Page Width |
| `Alt+V, Z, H` | Whole Page |

### Calx

| keys | row |
|---|---|
| `Alt+D, A` | Sort Ascending |
| `Alt+D, D` | Sort Descending |
| `Alt+D, S` | Sort… |
| `Alt+D, F` | Filter |
| `Alt+D, C` | Clear Filter *(disabled here)* |
| `Alt+D, R` | Reapply Filter *(disabled here)* |
| `Alt+D, T` | Text to Columns… |
| `Alt+D, U` | Remove Duplicates… |
| `Alt+E, U` | Undo *(disabled here)* |
| `Alt+E, R` | Redo *(disabled here)* |
| `Alt+E, T` | Cut |
| `Alt+E, C` | Copy |
| `Alt+E, P` | Paste |
| `Alt+E, S` | Paste Special… |
| `Alt+E, N` | Clear Contents |
| `Alt+E, F` | Find… |
| `Alt+E, E` | Replace… |
| `Alt+E, G` | Go To… |
| `Alt+F, N` | New |
| `Alt+F, O` | Open… |
| `Alt+F, R` | Open Recent ▸ *(disabled here)* |
| `Alt+F, S` | Save |
| `Alt+F, A` | Save As… |
| `Alt+F, C` | Close |
| `Alt+F, X` | Exit |
| `Alt+I, R` | Rows |
| `Alt+I, C` | Columns |
| `Alt+I, W` | Delete Rows |
| `Alt+I, M` | Delete Columns |
| `Alt+I, H` | Chart ▸ |
| `Alt+I, P` | Picture… |
| `Alt+I, S` | Sum Above |
| `Alt+I, N` | Note |
| `Alt+I, H, O` | Column ▸ |
| `Alt+I, H, B` | Bar ▸ |
| `Alt+I, H, L` | Line |
| `Alt+I, H, A` | Area ▸ |
| `Alt+I, H, E` | Pie |
| `Alt+I, H, D` | Doughnut |
| `Alt+I, H, S` | Scatter |
| `Alt+I, H, R` | Radar |
| `Alt+I, H, A, N` | Standard |
| `Alt+I, H, A, S` | Stacked |
| `Alt+I, H, A, K` | 100% Stacked |
| `Alt+I, H, B, C` | Clustered |
| `Alt+I, H, B, S` | Stacked |
| `Alt+I, H, B, K` | 100% Stacked |
| `Alt+I, H, O, C` | Clustered |
| `Alt+I, H, O, S` | Stacked |
| `Alt+I, H, O, K` | 100% Stacked |
| `Alt+O, C` | Cells… |
| `Alt+O, R` | Row ▸ |
| `Alt+O, N` | Column ▸ |
| `Alt+O, S` | Sheet ▸ |
| `Alt+O, I` | Fill Colour ▸ |
| `Alt+O, M` | Merge Cells |
| `Alt+O, D` | Conditional Formatting… |
| `Alt+O, F` | Clear Formatting |
| `Alt+O, I, —` | No Fill |
| `Alt+O, N, W` | Width… |
| `Alt+O, N, I` | Fit to Contents |
| `Alt+O, N, H` | Hide |
| `Alt+O, N, U` | Unhide |
| `Alt+O, R, T` | Height… |
| `Alt+O, R, I` | Fit to Contents |
| `Alt+O, R, H` | Hide |
| `Alt+O, R, U` | Unhide |
| `Alt+O, S, I` | Insert |
| `Alt+O, S, R` | Rename… |
| `Alt+O, S, M` | Move or Copy… |
| `Alt+O, S, D` | Delete *(disabled here)* |
| `Alt+O, S, H` | Hide *(disabled here)* |
| `Alt+O, S, U` | Unhide All *(disabled here)* |
| `Alt+T, P` | Protect Sheet |
| `Alt+T, D` | Data Validation… |
| `Alt+T, N` | Define Names… |
| `Alt+V, F` | Freeze Panes |
| `Alt+V, P` | Split |
| `Alt+V, Z` | Zoom ▸ |
| `Alt+V, Z, —` | 50% |
| `Alt+V, Z, —` | 75% |
| `Alt+V, Z, —` | 100% |
| `Alt+V, Z, —` | 125% |
| `Alt+V, Z, —` | 150% |
| `Alt+V, Z, —` | 200% |
