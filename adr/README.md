# Architecture decision records

One file per decision that shaped this codebase in a way the code alone cannot
explain. A record says what was decided, what forced the decision, and what it
costs — so that the next person (or the same person, a year on) does not
quietly re-litigate it without the evidence that settled it.

Records are numbered and never rewritten; a superseded decision gets a new
record pointing back.
