[package]
name = "ss-xls"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
ss-model.workspace = true
cfb-reader.workspace = true

[dev-dependencies]
# The fixture builder, so the tests can lay out a .xls from the specification
# rather than depend on a file nobody in this repository is allowed to ship.
cfb-reader = { workspace = true, features = ["test-support"] }
